<div class="page-width">
  <h1>404</h1>
  <div class="rte">
    <p>{{ 'general.404.title' | t | default: 'Page not found' }}</p>
    <p><a href="{{ routes.root_url }}">{{ 'general.404.link' | t | default: 'Continue shopping' }}</a></p>
  </div>
</div>
